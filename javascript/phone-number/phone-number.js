"use strict"

module.exports = class {
   number(input){
     let phoneNumber;
     phoneNumber
   }
}

function checknumber(string) {
  let number;
  string.filter(function(item) {
    number = item.isNumber()
  });

  return number
}
